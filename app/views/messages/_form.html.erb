<%= simple_form_for @message do |f| %>
  <% if message.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
        <% message.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.association :recipient, label: 'Destinatário', :collection => User.all.map{ |l| [l.username, l.id, {:username => l.username.downcase}] } %>    
  </div>
  
  <div>
    <%= f.label 'Mensagem', style: "display: block" %>
    <%= f.text_area :content %>
  </div>

  <div>
    <%= f.submit "Enviar" %>
  </div>
<% end %>
